/**
 * Electron preload script for Neon Flappy.
 * contextIsolation is enabled; the game uses no Node.js APIs,
 * so this preload intentionally exposes nothing extra.
 */
// No-op: game is pure browser JS (canvas, localStorage, requestAnimationFrame)
